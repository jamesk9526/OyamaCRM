diff --git a/server/src/services/oyama-donor-profile.ts b/server/src/services/oyama-donor-profile.ts
--- a/server/src/services/oyama-donor-profile.ts
+++ b/server/src/services/oyama-donor-profile.ts
@@ -102,7 +102,16 @@ function normalized(value: string | null | undefined): string {
 }
 
 function compact(value: string | null | undefined): string {
-  return normalized(value).replace(/\s+/g, "");
+  return normalized(value)
+    .replace(/\bstreet\b/g, "st")
+    .replace(/\broad\b/g, "rd")
+    .replace(/\bavenue\b/g, "ave")
+    .replace(/\bboulevard\b/g, "blvd")
+    .replace(/\bdrive\b/g, "dr")
+    .replace(/\blane\b/g, "ln")
+    .replace(/\bhighway\b/g, "hwy")
+    .replace(/\broute\b/g, "rte")
+    .replace(/\s+/g, "");
 }
