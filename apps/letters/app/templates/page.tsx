/** Standalone Oyama Letters template library route. */
import LetterTemplatesList from "@/components/letters/LetterTemplatesList";

/** Opens the reusable document template library. */
export default function LettersTemplatesPage() {
  return <LetterTemplatesList />;
}
