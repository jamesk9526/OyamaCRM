// Watchdog runbooks route entry.
import WatchdogRunbooksPage from "@/app/components/watchdog/ops/WatchdogRunbooksPage";

/** Renders Watchdog recovery runbook catalog. */
export default function WatchdogRunbooksRoutePage() {
  return <WatchdogRunbooksPage />;
}
