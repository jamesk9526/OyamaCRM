/** Full-screen spreadsheet workspace for large Contacts Manager list work. */
import ContactsManagerPage from "@/app/components/contacts-manager/ContactsManagerPage";

/** Renders Contacts Manager in dense spreadsheet mode for large list selection and cleanup. */
export default function ContactsManagerFullscreenRoute() {
  return <ContactsManagerPage fullscreen />;
}
