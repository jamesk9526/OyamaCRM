/** Contacts Manager route for shared email and printable audience lists. */
import ContactsManagerPage from "@/app/components/contacts-manager/ContactsManagerPage";

/** Renders the shared audience/contact manager used by Communications and Letters & Printables. */
export default function ContactsManagerRoute() {
  return <ContactsManagerPage />;
}
