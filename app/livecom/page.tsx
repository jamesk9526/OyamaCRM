// DonorCRM LiveCom route entry for donor interaction workflows.

import LiveComWorkspace from "@/app/components/livecom/LiveComWorkspace";

/**
 * LiveComPage renders the first-pass LiveCom workspace in DonorCRM.
 */
export default function LiveComPage() {
  return <LiveComWorkspace />;
}
