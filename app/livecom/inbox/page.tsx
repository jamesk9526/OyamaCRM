// Direct LiveCom Inbox route for staff who need the messenger queue immediately.

import LiveComInboxTool from "@/app/components/livecom/LiveComInboxTool";

/** Renders the standalone LiveCom Inbox tool at /livecom/inbox. */
export default function LiveComInboxPage() {
  return <LiveComInboxTool />;
}
