import WebmasterEditorWorkspace from "@/app/components/webmaster/editor/WebmasterEditorWorkspace";

/** Full-page visual editor route for Webmaster website editing workflows. */
export default function WebmasterEditorPage() {
  return <WebmasterEditorWorkspace />;
}
