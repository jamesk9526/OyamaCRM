/** OyamaWebMaster dashboard route entrypoint. */
import WebmasterStarterDashboard from "@/app/components/webmaster/WebmasterStarterDashboard";

/** WebmasterPage renders the primary dashboard for OyamaWebMaster. */
export default function WebmasterPage() {
  return <WebmasterStarterDashboard />;
}
