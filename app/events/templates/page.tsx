// Page entry for the global Events template management workspace.
import { EventsTemplatesWorkspace } from "@/app/components/events/EventsTemplatesWorkspace";

/**
 * EventsTemplatesRoute renders template creation workflows that remain global to all events.
 */
export default function EventsTemplatesRoute() {
  return <EventsTemplatesWorkspace />;
}
