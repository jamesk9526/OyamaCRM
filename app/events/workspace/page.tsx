/** Events workspace selector route enforces event-first navigation into scoped tools. */

import EventsWorkspaceSelectorPage from "@/app/components/events/EventsWorkspaceSelectorPage";

/** EventsWorkspaceRoute renders the workspace selector for choosing event and tool scope. */
export default function EventsWorkspaceRoute() {
  return <EventsWorkspaceSelectorPage />;
}
