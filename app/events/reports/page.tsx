/** Reports workspace route for Events CRM. */

import EventReportsContent from "./EventReportsContent";

/**
 * EventReportsPage displays real event analytics, revenue breakdowns,
 * and post-event follow-up reporting with donor sync insights.
 */
export default function EventReportsPage() {
  return <EventReportsContent />;
}
