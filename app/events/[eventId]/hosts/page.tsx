// Event-scoped hosts route wrapper for /events/[eventId]/hosts.

import EventHostsPage from "@/app/events/hosts/page";

/**
 * EventWorkspaceHostsPage renders the hosts workspace in event-scoped routing.
 */
export default function EventWorkspaceHostsPage() {
  return <EventHostsPage />;
}
