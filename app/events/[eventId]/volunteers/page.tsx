// Event-scoped volunteers route wrapper for /events/[eventId]/volunteers.

import EventVolunteersPage from "@/app/events/volunteers/page";

/**
 * EventWorkspaceVolunteersPage renders the volunteers tool inside event workspace routing.
 */
export default function EventWorkspaceVolunteersPage() {
  return <EventVolunteersPage />;
}
