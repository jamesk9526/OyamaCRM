// Event-scoped tools inherit the canonical Event Operations shell from the parent layout.
export default function EventWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
