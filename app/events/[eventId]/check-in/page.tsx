// Event-scoped check-in route wrapper for /events/[eventId]/check-in.

import EventCheckInPage from "@/app/events/check-in/page";

/**
 * EventWorkspaceCheckInPage renders the check-in tool inside event workspace routing.
 */
export default function EventWorkspaceCheckInPage() {
  return <EventCheckInPage />;
}
