// Event-scoped fundraising route wrapper for /events/[eventId]/fundraising.

import EventFundraisingPage from "@/app/events/fundraising/page";

/**
 * EventWorkspaceFundraisingPage renders the fundraising tool inside event workspace routing.
 */
export default function EventWorkspaceFundraisingPage() {
  return <EventFundraisingPage />;
}
