// Event-scoped tables route wrapper for /events/[eventId]/tables.

import EventTablesPage from "@/app/events/tables/page";

/**
 * EventWorkspaceTablesPage renders the tables tool inside event workspace routing.
 */
export default function EventWorkspaceTablesPage() {
  return <EventTablesPage />;
}
