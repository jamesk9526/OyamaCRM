// Event-scoped sponsors route wrapper for /events/[eventId]/sponsors.

import EventsSponsorsPage from "@/app/events/sponsors/page";

/**
 * EventWorkspaceSponsorsPage renders the sponsors tool inside event workspace routing.
 */
export default function EventWorkspaceSponsorsPage() {
  return <EventsSponsorsPage />;
}
