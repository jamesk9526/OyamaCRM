// Event-scoped follow-up route wrapper for /events/[eventId]/follow-up.

import EventFollowUpPage from "@/app/events/follow-up/page";

/**
 * EventWorkspaceFollowUpPage renders the follow-up workspace in event-scoped routing.
 */
export default function EventWorkspaceFollowUpPage() {
  return <EventFollowUpPage />;
}
