// Event-scoped settings route wrapper for /events/[eventId]/settings.

import EventSettingsPage from "@/app/events/settings/page";

/**
 * EventWorkspaceSettingsPage renders the settings tool inside event workspace routing.
 */
export default function EventWorkspaceSettingsPage() {
  return <EventSettingsPage />;
}
