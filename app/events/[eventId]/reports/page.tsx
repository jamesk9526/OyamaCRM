// Event-scoped reports route wrapper for /events/[eventId]/reports.

import EventReportsPage from "@/app/events/reports/page";

/**
 * EventWorkspaceReportsPage renders the reports tool inside event workspace routing.
 */
export default function EventWorkspaceReportsPage() {
  return <EventReportsPage />;
}
