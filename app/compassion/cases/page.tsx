/** Compassion CRM cases route composed from the production case workspace. */
import CasesWorkspace from "@/app/components/compassion/cases/CasesWorkspace";

/** Renders the case management workspace inside the Compassion shell. */
export default function CompassionCasesPage() {
  return <CasesWorkspace />;
}
