/** Compassion CRM dashboard route composed from the workflow dashboard workspace. */
import CompassionDashboardWorkspace from "@/app/components/compassion/dashboard/CompassionDashboardWorkspace";

/** Renders the Compassion dashboard inside the module shell. */
export default function CompassionDashboardPage() {
  return <CompassionDashboardWorkspace />;
}
