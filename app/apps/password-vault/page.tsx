// Standalone Password Vault app route entry.
import OyamaPasswordWorkspace from "@/app/components/password/OyamaPasswordWorkspace";

/** Renders the OyamaPASSWORD vault workspace under the standalone apps shell. */
export default function PasswordVaultAppPage() {
  return <OyamaPasswordWorkspace />;
}
