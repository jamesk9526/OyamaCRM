import StripeIntegrationWorkspace from "@/app/components/integrations/stripe/StripeIntegrationWorkspace";

/** Canonical Donor CRM Stripe Giving integration workspace. */
export default function StripeIntegrationPage() {
  return <StripeIntegrationWorkspace />;
}
