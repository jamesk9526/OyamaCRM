/** OyamaLetters settings route. */
import OyamaLettersWorkspace from "@/app/components/letters/OyamaLettersWorkspace";

/** Renders live letter preset and signature management inside the OyamaLetters space. */
export default function OyamaLettersSettingsPage() {
  return <OyamaLettersWorkspace view="settings" />;
}
