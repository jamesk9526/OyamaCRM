/** Canonical standalone OyamaLetters workspace route. */
import OyamaLettersWorkspace from "@/app/components/letters/OyamaLettersWorkspace";

/** Opens the dedicated OyamaLetters template library workspace. */
export default function OyamaLettersPage() {
  return <OyamaLettersWorkspace view="library" />;
}
