/** Settings route for configuring Steward AI local/remote runtime options. */
import AISettingsPage from "@/app/components/settings/ai/AISettingsPage";

/** AISettingsRoute renders the full settings form for Steward AI provider configuration. */
export default function AISettingsRoute() {
  return <AISettingsPage />;
}
