/** Presets library route for letters-printables workflow. */
import LetterPresetsHub from "@/app/components/letters/LetterPresetsHub";

/** Renders reusable preset inventory and management launch points. */
export default function LetterPresetsPage() {
  return <LetterPresetsHub />;
}
