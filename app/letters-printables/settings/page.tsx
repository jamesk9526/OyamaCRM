/** Workflow settings route for letters operations. */
import LetterWorkflowSettingsPage from "@/app/components/letters/LetterWorkflowSettingsPage";

/** Renders letters workflow settings and readiness guidance. */
export default function LettersWorkflowSettingsPageRoute() {
  return <LetterWorkflowSettingsPage />;
}
