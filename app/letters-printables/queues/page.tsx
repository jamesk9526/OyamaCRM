/** Canonical route for the unified letters production/print/mail queues workspace. */
import LettersQueuesWorkspace from "@/app/components/letters/LettersQueuesWorkspace";

/** Renders the ribbon-first queue workspace. */
export default function LettersQueuesPage() {
  return <LettersQueuesWorkspace />;
}
