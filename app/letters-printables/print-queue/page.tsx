/** Print queue route for letters operations. */
import LetterPrintQueue from "@/app/components/letters/LetterPrintQueue";

/** Renders the print queue management workspace. */
export default function LettersPrintQueuePage() {
  return <LetterPrintQueue />;
}
