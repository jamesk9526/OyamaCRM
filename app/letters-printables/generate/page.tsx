/** Letter generation center route. */
import LetterGenerateCenter from "@/app/components/letters/LetterGenerateCenter";

/** Renders one-off generation workflow with merge preview. */
export default function LettersGeneratePage() {
  return <LetterGenerateCenter />;
}
